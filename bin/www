#!/usr/bin/env node
var debug = require('debug')('projet elsa');
var app = require('../app');

app.set('port', process.env.PORT || 80);

var server = app.listen(app.get('port'), function() {
	console.log(server.address());
	debug('Express server listening on port ' + server.address().port);
});